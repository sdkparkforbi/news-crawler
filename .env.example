# 수집·본문추출만 쓰면 비밀키가 전혀 필요 없습니다.
# 아래는 private repo 를 Colab에서 클론할 때만 쓰는 GitHub 토큰입니다.
# (Colab Secrets 🔑 에 GH_TOKEN 으로 넣으면 .env 없이도 동작)

# GitHub Personal Access Token (repo read 권한). private repo 클론용.
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── 분석 노트북(뉴스분석)용 ──
# bge-m3 임베딩 서버(미들턴). 자체 서버를 쓰면 주소만 바꾸면 됩니다.
MIDDLETON_URL=https://middleton.p-e.kr/api/embeddings
MIDDLETON_API_KEY=                  # bge-m3(①②) 임베딩 키
OPENAI_API_KEY=sk-xxxxxxxx          # 필터링(gpt-4o-mini)·3large(③④) 임베딩
# 모델명(보통 그대로)
BGE_MODEL=bge-m3:latest
OAI_EMB_MODEL=text-embedding-3-large
CLASSIFY_MODEL=gpt-4o-mini
